Steg för steg RegisterDB!!!

🔹 Steg 1 – Skapa en Resource Group

Gå till Azure Portal
.

Klicka på Create a resource → Resource Group.

Döp den till t.ex. visitor-registration-rg.

Välj en region nära dig (t.ex. North Europe).

Klicka Review + Create → Create.

🔹 Steg 2 – Skapa databasen

I portalen, klicka Create a resource → sök Azure SQL Database.

Skapa en SQL Database:

Database name: VisitorDB

Server: Skapa ny → t.ex. visitor-sql-server

Ange användarnamn och lösenord (spara dessa!).

Pricing tier: välj Basic (billigast för test).

Klicka Review + Create → Create.

👉 När databasen är klar:

Gå till VisitorDB → Query editor → logga in med ditt användarnamn/lösenord.

Kör detta SQL för att skapa tabell:

CREATE TABLE Visitors (
    Id INT IDENTITY PRIMARY KEY,
    Name NVARCHAR(100),
    CheckInTime DATETIME DEFAULT GETDATE()
);

🔹 Steg 3 – Skapa backend (Azure Functions)

I portalen, Create a resource → sök Function App.

Välj:

Name: visitor-function-app

Publish: Code

Runtime stack: Node.js eller .NET (båda funkar, ta Node.js om du vill ha enklare JS-kod).

Region: samma som databasen.

Plan: Consumption (serverless).

Klicka Review + Create → Create.

👉 När Function App är skapad:

Gå till Functions → Create → välj HTTP Trigger.

Namnge funktionen RegisterVisitor.

Klicka på den → gå in på Code + Test.

Klistra in exempel (Node.js):

const { Connection, Request } = require("tedious");

module.exports = async function (context, req) {
    const name = req.body?.name || "Unknown";

    // Logga till Application Insights
    context.log("Ny registrering: " + name);

    // Koppla mot databasen
    const config = {
        authentication: {
            options: {
                userName: process.env["DB_USER"],
                password: process.env["DB_PASS"]
            },
            type: "default"
        },
        server: process.env["DB_SERVER"],
        options: {
            database: process.env["DB_NAME"],
            encrypt: true
        }
    };

    const connection = new Connection(config);

    await new Promise((resolve, reject) => {
        connection.on("connect", err => {
            if (err) {
                context.log("Connection failed", err);
                reject(err);
            } else {
                const sql = `INSERT INTO Visitors (Name) VALUES (@Name);`;
                const request = new Request(sql, err => {
                    if (err) reject(err);
                    else resolve();
                });
                request.addParameter("Name", TYPES.NVarChar, name);
                connection.execSql(request);
            }
        });
        connection.connect();
    });

    context.res = {
        body: { message: `Registrering mottagen för ${name}` }
    };
};


👉 Viktigt:

Gå till Configuration i Function App → Lägg till Application settings:

DB_USER = ditt SQL användarnamn

DB_PASS = lösenordet

DB_SERVER = t.ex. visitor-sql-server.database.windows.net

DB_NAME = VisitorDB

🔹 Steg 4 – Skapa frontend (Static Web App)

Create a resource → sök Static Web App.

Name: visitor-frontend.

Deployment: välj Other (ingen GitHub än).

Region: samma som backend.

Klicka Create.

👉 När sidan är klar:

Gå till Configuration → ställ in API URL till din Function App.

Ladda upp en enkel index.html:

<!DOCTYPE html>
<html>
<head>
    <title>Besöksregistrering</title>
</head>
<body>
    <h1>Registrera dig som besökare</h1>
    <form id="visitorForm">
        <input type="text" id="name" placeholder="Ditt namn" required>
        <button type="submit">Checka in</button>
    </form>
    <p id="result"></p>

    <script>
        document.getElementById("visitorForm").addEventListener("submit", async (e) => {
            e.preventDefault();
            const name = document.getElementById("name").value;
            const response = await fetch("DIN_FUNCTION_URL", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ name })
            });
            const data = await response.json();
            document.getElementById("result").innerText = data.message;
        });
    </script>
</body>
</html>


👉 Byt DIN_FUNCTION_URL till länken du hittar i Function App → Get function URL.

🔹 Steg 5 – Testa systemet

Öppna din Static Web App URL.

Skriv in ett namn och skicka formuläret.

Gå till SQL Database → Query editor → SELECT * FROM Visitors;

Gå till Application Insights (kopplat till Function App) → se loggar.